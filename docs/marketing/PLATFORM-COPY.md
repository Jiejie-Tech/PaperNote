# PaperNote 平台宣传文案

发布时请把项目地址、下载地址、版本号和设备截图替换为实际内容。

## B站

### 标题

我做了一款支持电脑、手机和平板的开源手写笔记｜PaperNote

### 简介

PaperNote 是一款本地优先、免费开源的手写笔记与 PDF 批注应用，目前支持 Windows 10/11 和 Android 7.0 以上设备。

当前功能包括：

- 钢笔、荧光笔、橡皮擦、撤销重做和触控笔压力输入
- 空白、点阵、横线、方格等纸张模板
- 页面新增、复制、删除、命名和快速跳转
- PDF 导入、手写批注与重新导出
- 文本、图片和七类常用形状
- 搜索、回收站、自动保存、单笔记导入导出和整库备份恢复
- Windows 与 Android 共用 `.papernote` 和 PaperInk 墨迹格式

所有核心笔记数据默认保存在设备本地，不需要账号。Android 客户端不申请网络权限。PDF 文本搜索适用于自带文本层的文件；扫描版 PDF 可逐页运行离线 OCR，暂不支持整本批量识别。当前版本也不包含账号云同步或多人协作。

项目源码：GitHub / Gitee 搜索 PaperNote
下载位置：项目 Releases 中的 Windows 包、Android ZIP 与 Android APK

推荐标签：开源软件、手写笔记、Android、Windows、触控笔、平板、PDF批注、效率工具

## V2EX「分享创造」

### 标题

[分享创造] PaperNote：支持 Windows 和 Android 的本地优先手写笔记应用

### 正文

最近完成了 PaperNote 的 Android 客户端和跨平台数据层。

PaperNote 是一款本地优先的手写笔记与 PDF 批注应用。Windows 版适合触控电脑和数位板，Android 版适配手机和平板，并使用原生触控输入采集压力、倾角和触控笔橡皮端。两个平台可以通过 `.papernote` 文件和整库备份交换数据。

目前支持资料库、搜索、回收站、分页和模板、钢笔/荧光笔/橡皮擦、撤销重做、文本/图片/形状、PDF 文本搜索/目录/内部链接/批注列表、页面范围导出与提取，以及本地自动保存。Android 默认可直接手指书写，也可关闭手指书写后用单指移动；双指始终可缩放和平移。

技术栈为 .NET 10、WPF、.NET MAUI 和 Android 原生 MotionEvent。仓库包含不操控用户鼠标的 Windows 隐藏界面测试，以及 ADB 驱动的 Android 安装、书写、页面、返回键和持久化测试。

当前没有账号云同步或多人协作。自带文本层的 PDF 可直接离线搜索；扫描页可逐页运行随安装包提供的离线 OCR，大型 PDF 已支持进度、取消和续接。希望获得不同触控笔设备、低内存手机、500 页/200 MB 真实讲义和首次使用体验方面的反馈。

项目与下载地址：发布时粘贴 GitHub / Gitee 地址。

## 知乎 / 掘金 / CSDN

### 标题

从 WPF 到 Android：PaperNote 如何共享笔记数据与跨平台墨迹

### 内容提纲

1. 为什么选择本地优先和文件交换；
2. 从桌面单体到 `PaperNote.Core` 的拆分；
3. ISF 与 PaperInk 双格式兼容；
4. Android MotionEvent 的压力、倾角和橡皮端；
5. 手机和平板响应式页面导航；
6. 自动保存并发问题和返回键异常修复；
7. 后台自动化测试与签名 APK 发布；
8. 后续性能、可访问性和同步方向。

## 小红书 / 即刻 / 朋友圈

### 短版

PaperNote 现在支持 Windows 和 Android：触控笔书写、几何规整、混合选区 PNG、分页模板、对象与图层、PDF 文本搜索/目录/内部链接/批注列表、页面范围导出与提取、本地录音波形与笔迹同步回放、恢复中心和带校验的备份都能离线使用。电脑与手机/平板可以直接传 `.papernote` 文件，不需要注册账号。源码和安装包已放到 GitHub / Gitee。

### 更新版

PaperNote Android 1.0.0：

- 手机和平板响应式界面
- 原生触控笔压力书写
- 单指平移、双指缩放
- PDF 页码范围导入、进度、取消续接、文本搜索、目录、内部链接、批注列表和范围导出/提取
- 直线/45°/矩形/椭圆规整与混合选区 PNG 分享
- 与 Windows 交换笔记文件
- 本地录音波形、分段跳转、播放笔迹高亮、草稿恢复与损坏文件抢救
- 本地保存，无内置网络权限

项目地址与 APK：发布时补充。

## 英文仓库简介

Local-first handwriting and PDF study app for Windows and Android, with offline text search, outlines, internal links, annotations and page extraction. Built with .NET 10, WPF and .NET MAUI.

## 推荐 Topics

`android` `windows` `dotnet` `dotnet-maui` `wpf` `handwriting` `stylus` `pdf-annotation` `offline-first` `note-taking`

### 2026-07-23 离线完整版更新

PaperNote 现已补齐整本离线 OCR 与校对、PDF 原文复制和字词坐标、覆盖层表单/图片签名/比例测量、基础 LaTeX 公式、笔记本合并、双页/参考阅读、几何标尺、素材模板资源包、安全声明式扩展和录音精细时间轴。所有处理继续留在本机；Android 不申请网络权限。

需要诚实说明：公式识别是基础规则；PDF 表单和签名不会写回原 PDF，签名也不是证书数字签名；双页/参考视图以只读为主；低内存 Android 和多品牌触控笔仍需更多长期真机验证。

## Implementation status — 2026-07-23

本节是当前离线版本能力边界的统一说明。

### 已实现并纳入仓库测试

- Windows 与 Android 共用 PaperInk、页面对象和格式 19 数据模型；支持压力、倾角、平滑、整笔/局部擦除、透明度、图层、对象组合、锁定、层级和自由形状混合套索。
- 几何工具覆盖直线与 45° 吸附、矩形/椭圆规整、长度测量、可设长度和角度的标尺线、角度辅助线、平行线及垂线构造。
- PDF 支持 200 MB/500 页边界、范围导入、进度/取消/失败续接、SHA-256 缓存、原文与字词坐标提取、搜索/复制、目录、内部链接、评论、批注筛选、页面旋转、范围导出与提取。
- 高级 PDF 覆盖层支持文本填写框、复选框、必填校验、图片/选区签名和测量比例校准；可合并本地笔记本并安全重映射页面、图层、笔迹、对象、链接、目录和录音时间点 ID。
- 本地识别支持当前页与整本批量 OCR、进度/取消、区域/置信度/校对状态保存、选区手写转文字；基础数学规则可生成并编辑 LaTeX，复制公式并导出 SVG。
- Windows 提供双页只读阅读窗口和独立参考页窗口；Android 提供双页/参考页只读视图，返回后继续编辑原页。两端均可保存工具栏布局，移动端关键按钮保持至少 48 dp 触控尺寸。
- 个人素材库和共享模板支持分类、搜索、收藏及本地资源包导入/导出；声明式本地扩展只允许受限文字操作，不加载 DLL、不运行脚本或第三方代码。
- 页面录音支持波形、播放/暂停、时间标记、笔迹双向跳转、标记移动/重命名/删除和非破坏性播放裁剪；Android 另提供省空间、标准、高质量三档录音质量。
- 保存采用临时文件验证后替换；恢复中心、只读损坏检查、历史版本、整库备份格式 3、PNENC2 笔记级密码保护和 PNENC1 兼容均已纳入测试。
- 当前 `.papernote` 内部格式为 `FormatVersion 19`：格式 17 引入录音波形，格式 18 引入批量 OCR 元数据，格式 19 引入 PDF 字词坐标/测量比例、表单/签名/公式元数据和录音质量/裁剪字段。
- 后台回归覆盖 Core、格式 19 往返、PDF/公式/合并/资源包/安全扩展、Windows 隐藏 UI、Android Release 编译与 APK 静态检查；整个测试过程不操控用户鼠标。

### 当前明确边界

- 数学识别是基础规则转换，不是成熟公式识别模型；复杂手写公式仍需手动编辑 LaTeX。
- PDF 表单、签名和测量以 PaperNote 页面覆盖层保存，不直接写回原 PDF 的 AcroForm；本地签名是图片签名，不是证书型数字签名。
- PDF 内部链接仅在目标页已导入当前笔记时可跳转；原文可查看和复制，但尚未提供逐字选择、原生文本高亮或直接修改原 PDF。
- Windows 录音使用系统默认录音设备，目前没有可靠的设备枚举和三档编码质量；Android 已提供三档质量。播放裁剪不会改写原音频文件。
- 双页与参考视图是只读辅助阅读：Windows 主窗口可继续编辑，Android 返回编辑器后继续编辑原页，不是同屏双画布编辑。
- 扩展系统只执行声明式文字命令；不会加载不受信任代码。无障碍提供基础语义、键盘焦点和高对比度资源，但未宣称通过完整 WCAG、TalkBack 或 Narrator 专项认证。
- 200 MB/500 页边界已自动化验证；低内存 Android、多品牌触控笔、超长录音和长时间运行仍需要持续真机压力测试。
- 账号、云同步、联网 AI、遥测、广告、多人协作和在线商店不在本地离线版本范围内。
- APK、ZIP、签名密钥、构建输出和私人笔记属于发布产物或本地数据，不提交到源码仓库。

### 验证入口

```powershell
.\scripts\test.ps1 -SkipAndroidRuntime
.\scripts\build-release.ps1 -SkipTests
.\scripts\build-android.ps1
.\scripts\test-android.ps1 -SkipBuild -SkipUi
```

## 可用于平台介绍的密码保护文案

**单本笔记本地密码保护**：Windows 与 Android 共用离线加密格式，可为重要笔记设置密码；页面、笔迹、PDF 内容和录音信息加密保存在本机，自动保存、历史版本与整库备份会保持保护状态。密码不会上传或保存，忘记后无法找回。
