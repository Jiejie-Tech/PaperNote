# PaperNote

PaperNote Desktop 是一款独立设计、面向 Windows 笔记本电脑的本地优先墨迹笔记应用。项目使用 .NET 9、WPF、Windows InkCanvas 和本地 JSON 文件，适合触控笔、数位板和鼠标使用。所有核心笔记数据默认保存在本机，不依赖账号、订阅或云服务。

> 当前数据格式版本：13  
> 当前状态：可运行的单机离线版本。产品功能路线图中的本地核心能力已集中完成；账号、云同步、在线协作和服务端能力明确不在当前范围内。

## 项目定位

- 面向 Windows 笔记本电脑、触控设备和数位板使用场景。
- 围绕手写、分页、纸张模板、PDF 批注和本地资料库提供完整工作流。
- PaperNote 使用自有名称、界面、图标和文档资源，代码由本项目独立维护。

### 界面预览

![PaperNote 本地资料库](docs/screenshots/PaperNote资料库.png)

![PaperNote 笔记编辑器](docs/screenshots/PaperNote编辑器.png)

## 已完成功能

### 书写与编辑

- 圆珠笔、钢笔、画笔、铅笔、荧光笔、橡皮、浏览和套索工具
- 压感曲线支持关闭、柔和、标准和强烈；笔迹平滑支持关闭、标准和强力
- 内置细黑圆珠笔、蓝色钢笔、红色批注笔和黄色荧光笔快捷预设
- 颜色、粗细、橡皮尺寸、缩放、全屏和只读模式
- 笔迹撤销、重做、清空、ISF 导入和 ISF 导出
- 区域混合套索可同时选中笔迹、文本、图片和形状
- 按住 `Ctrl` 可追加混合选区
- 自动保存，关闭窗口前会以非阻塞方式再次保存；连续点击关闭也不会重复进入保存流程

### 笔记本与页面

- 独立设计的本地资料库、封面、文件夹、分类和回收站
- 新建、打开、重命名、移动到回收站、恢复和永久删除笔记本
- 页面缩略图、添加、复制、删除、上移和下移
- 添加页面可选择插入到当前页前、当前页后或笔记末尾，并可直接选择空白、点阵、横线或方格纸
- `Ctrl+Enter` 沿用当前纸张，在当前页后快速插入一页
- 可把当前模板和颜色收藏为“常用纸张”，最多 8 个，并在当前页前、后或笔记末尾快速插入
- 每页可设置独立标题，并在侧栏直接添加或取消书签
- 页面搜索支持页码、页面标题和页面内文字对象
- “仅书签”筛选可快速定位重点页面
- 页面网格总览以大缩略图展示整本笔记，支持搜索、仅书签筛选和多选批量操作
- 页码输入框可直接跳转；底部按钮和 `PageUp` / `PageDown` 可快速前后翻页
- 底部提供页面访问后退/前进，可回到刚才浏览过的位置，并支持浏览器后退/前进硬件键
- 自动保存当前阅读页；再次打开笔记时恢复上次阅读位置
- 每页可设置一级、二级或三级目录级别；笔记目录支持搜索、缩进显示和双击跳转
- `Ctrl+J` 聚焦页码输入框，`Ctrl+Shift+O` 打开页面总览，`Ctrl+Shift+D` 打开笔记目录，`Esc` 关闭覆盖层
- 页面缩略图使用内存缓存，搜索、筛选和长笔记刷新时不再重复渲染未变化页面
- `Ctrl` / `Shift` 多选页面
- 多选页面稳定批量上移、下移、复制、删除、添加书签和取消书签
- 批量旋转 PDF 页面、批量裁边、清除裁剪和恢复原始状态
- 空白、点阵、横线和方格纸张；支持多种纸张颜色
- 可将 PNG、JPG、BMP、GIF 或 WebP 图片替换为当前页纸张，也可批量插入为独立图片页
- 共享模板库可保存当前页纸张，并跨笔记应用或插入；最多保留 24 个普通/图片纸张模板

### 页面对象

- 插入并编辑文本、图片、矩形、圆角矩形、椭圆、三角形、菱形、箭头和直线
- 对象移动、缩放、旋转、锁定、解锁和删除
- 对象复制、剪切、粘贴、组合、取消组合和图层调整
- 多对象对齐、等距分布、样式、透明度、文字大小和描边设置
- 文本、图片和形状对象可链接到本笔记任意页面；编辑模式下 `Ctrl+单击` 打开，只读模式下直接单击打开

### 多笔记切换

- 编辑器顶部显示真正可见的横向标签条，最多 8 本，可直接切换或单独关闭
- `Ctrl+Tab` / `Ctrl+Shift+Tab` 循环切换，`Ctrl+W` 关闭当前标签
- 返回书架后标签仍保留；删除或重命名笔记时同步更新标签
- 重新启动后恢复最近标签列表和书架排序设置

### PDF

- 导入多页 PDF，并转换为笔记本内部的 840×1188 PNG 背景
- 导入前可输入 `1-3,5,8-10` 等页码范围，支持全部、奇数页、偶数页快捷选择和最多 300 页限制
- 所选 PDF 页面可插入当前页前、当前页后或笔记末尾，只渲染实际选择的页面
- PDF 原文件移动或删除后，笔记仍能继续显示和批注
- 当前 PDF 页面支持左转、右转、5%/10% 裁边、清除裁剪和恢复原始状态
- 自定义裁剪编辑器支持四边独立比例、滑块精调和直接拖动裁剪框，取消时不会修改页面
- 导出整本笔记或多选页面为扁平 PDF
- 导出前可选择较小文件、标准或高清质量，以及 A4、A5 或 Letter 页面尺寸
- 导出内容包含纸张、模板、PDF 背景、笔迹、文本、图片和形状

### 历史版本与数据安全

- 覆盖保存前自动保护旧版本
- 自动版本最短间隔为 2 分钟，单本笔记最多保留 30 份
- 顶部“历史版本”可手动创建当前版本并恢复旧内容
- 恢复历史版本前会自动创建“恢复前保护点”
- 笔记和版本文件均采用临时文件写入后替换，避免半写入文件
- 永久删除笔记本时同步清理对应历史版本
- 可把整个资料库和全部历史版本导出为 `.papernote-library.zip`，并从备份包恢复
- 恢复前先校验包内所有笔记和历史版本；相同内容跳过，同名冲突作为副本导入，不直接覆盖

## 整库搜索与文字提取

- 书架支持搜索笔记本标题、文件夹、页面标题、可编辑文字对象和 PDF/图片来源文件名
- 多个关键词可跨字段组合匹配，并在卡片上显示命中位置
- 书架支持最近访问筛选，以及按最近修改、最近打开和标题排序
- 可复制当前页文字、复制整本笔记文字或导出整本为 TXT
- 当前文字提取只覆盖已有可编辑文本、页面链接说明和来源元数据；**不包含手写 OCR，也不把搜索描述为 OCR 搜索**

## 数据位置

默认数据全部保存在当前 Windows 用户的“文档”目录：

```text
文档\PaperNote\Notebooks   # 正式 .papernote 笔记本
文档\PaperNote\Backups     # 自动和手动历史版本
文档\PaperNote             # 旧版快速笔记数据
本地应用数据\PaperNote      # 标签恢复、书架排序和共享模板设置
```

笔记本文件是本地 JSON 格式的 `.papernote` 文件，笔迹使用 ISF 数据，图片和 PDF 页面背景使用内嵌 Base64 数据，因此单个笔记本文件可以独立保存。

## 运行

环境要求：Windows 10/11、.NET 9 SDK。

```powershell
dotnet run --project ./src/PaperNote.Desktop/PaperNote.Desktop.csproj
```

## 后台测试

测试不会操控鼠标和键盘，也不会打开可见的 PaperNote 主窗口。所有测试数据均写入系统临时目录中的独立文件夹，不会读取或修改正式笔记。

```powershell
dotnet build ./tests/BackgroundUiTest/BackgroundUiTest.csproj -c Release
dotnet run --project ./tests/BackgroundUiTest/BackgroundUiTest.csproj -c Release --no-build

dotnet build ./tests/SmokeTest/SmokeTest.csproj -c Release
dotnet run --project ./tests/SmokeTest/SmokeTest.csproj -c Release --no-build
```

覆盖内容包括：

- 隐藏 WPF 页面渲染、笔迹与对象操作、混合套索和只读模式
- 页面标题编辑、标题搜索、正文搜索、书签筛选和批量书签
- 页面网格总览、总览筛选、多选联动、缩略图缓存和缓存失效
- 页码输入跳转、越界保护、上一页和下一页切换
- PDF 页面变换、自定义四边裁剪、裁剪取消保护和页面批量工具
- PDF 页码范围解析、全部/奇数/偶数预设、插入位置与只读拦截
- PDF 导出范围、质量与页面尺寸设置
- 当前页前/后及笔记末尾插页、模板与颜色保留、只读模式拦截
- 常用纸张收藏、去重、8 个上限、快速插页和删除管理
- 一级/二级/三级目录、目录搜索、重排后页码刷新、跳转和只读保护
- 多笔记快速标签、页面访问后退/前进、内部页面链接及只读点击
- 不可见窗口真实关闭、未保存内容退出前落盘、连续关闭防重入、工作区状态保存和关闭后计时器停止
- 图片纸张元数据、隐藏渲染、入口可用性和清除保护
- 整库搜索、文字提取、多关键词匹配、扩展形状和可见横向标签条
- 工作区标签去重与 8 本上限、书架排序恢复、共享模板规范化与 24 个上限
- 整库备份包清单、完整恢复、重复跳过、同名冲突副本、损坏包预检和历史版本映射
- 真实 PDF 导入导出、高清 Letter 导出、自动备份、手动版本和恢复前保护点
- 格式版本 13、最近打开时间、阅读位置、内部链接、扩展形状、图片纸张、旧格式兼容、损坏数据容错和临时文件清理

## 项目结构

```text
src/PaperNote.Desktop/                 WPF 主程序
src/PaperNote.Desktop/Models/          笔记本数据模型
src/PaperNote.Desktop/Services/        保存、备份、PDF、缩略图和笔迹服务
tests/BackgroundUiTest/                隐藏 WPF 界面测试
tests/SmokeTest/                       存储与真实 PDF 冒烟测试
docs/PaperNote产品与开发方案.md   产品和开发方案
```

## 范围边界与后续维护

P0～P2 已完成。后续开发以稳定性、易用性和性能优化为主，不再把以下 P3 能力纳入本项目：

- 账号体系、登录和跨设备云同步
- 多人实时协作、共享链接和在线评论
- 服务端存储、在线 OCR、AI 摘要或联网内容处理

当前标签模型仍是“保存后快速切换”，不是让多个未保存文档同时常驻内存。应用继续坚持私人、单机、离线和本地文件可控。


## 2026-07-20 稳定性修复

- 修复关闭窗口时在界面线程同步等待异步保存造成的卡死；关闭流程改为“先取消本次关闭、异步保存、保存完成后再安全关闭”。
- 关闭过程中暂时禁止继续编辑，避免保存途中新增内容却没有被标记。
- 自动保存、手动保存和关闭前保存使用同一串行入口，避免并发写入和临时文件争用。
- 连续点击关闭只执行一次保存；关闭完成后停止自动保存计时器并解除笔迹事件。
- 工作区状态保存失败不再阻止笔记内容保存和程序退出。
- 修复关闭当前标签时保存失败可能从异步界面事件冒泡并导致程序退出的问题。
- 快速连续切换笔记时不再并发打开多个目标笔记。
- 启动读取过程中收到关闭请求会及时停止后续初始化，不再在窗口关闭后继续创建或刷新内容。


## 开源与许可证

PaperNote Desktop 的项目代码采用 [MIT License](LICENSE) 发布。第三方组件仍适用各自许可证，详见 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) 和 `legal/third-party/`。

公开发布前请使用 `scripts/test.ps1` 完成后台验证，再使用 `scripts/build-release.ps1` 生成带许可证文件的发布包。不要把 `artifacts/`、`dist/`、本地笔记或工作区状态提交到 Git。

## 隐私与安全

PaperNote Desktop 默认不要求账号，也不主动上传笔记内容。数据位置、备份方式和用户责任见 [PRIVACY.md](PRIVACY.md)，漏洞报告流程见 [SECURITY.md](SECURITY.md)。

## 参与贡献

欢迎提交问题和改进，开发约定见 [CONTRIBUTING.md](CONTRIBUTING.md)，社区行为约定见 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。
